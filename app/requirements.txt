# Drugs@FDA Explorer — minimal deps for Posit Connect / local runs.
# Tip: keep this list small so the Connect manifest stays lean.
pandas>=2.0
plotly>=6.0
shiny>=1.5
shinyswatch>=0.10
requests>=2.28
python-dotenv>=1.0
